# 代码质量改进实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 改善 Prepress-toolbox 项目的代码质量，解决 God class、重复定义、空异常处理等问题

**Architecture:** 按优先级分阶段实施：先拆分核心 God class，再清理重复定义，最后修复异常处理。每个阶段独立可交付。

**Tech Stack:** C# 9.0, .NET Framework 4.8, xUnit, iText 9, Spire.Pdf

---

## 文件结构

### 新建文件
- `src/WindowsFormsApp3/Services/PdfCutPathExtractor.cs` - 裁切路径提取逻辑
- `src/WindowsFormsApp3/Services/PdfSpecialShapeProcessor.cs` - 异形处理逻辑
- `src/WindowsFormsApp3/Services/PdfLayerManager.cs` - PDF 图层管理

### 修改文件
- `src/WindowsFormsApp3/Utils/PdfTools.cs` - 拆分为上述三个服务
- `src/WindowsFormsApp3/Services/LoggerAdapter.cs` - 合并重复的 LoggerAdapter
- `src/WindowsFormsApp3/Utils/LoggerAdapter.cs` - 删除（与 Services 重复）
- `src/WindowsFormsApp3/Utils/ITextSharpLGPLPdfTools.cs` - 修复空 catch
- `src/WindowsFormsApp3/Utils/ThemeHelper.cs` - 修复空 catch
- `src/WindowsFormsApp3/Forms/Utils/FloatingDropZoneForm.cs` - 修复空 catch

---

## Task 1: 从 PdfTools.cs 提取裁切路径提取逻辑

**Files:**
- Create: `src/WindowsFormsApp3/Services/PdfCutPathExtractor.cs`
- Modify: `src/WindowsFormsApp3/Utils/PdfTools.cs:376-481`
- Test: `src/WindowsFormsApp3.Tests/Utils/PdfCutPathExtractorTests.cs`

- [ ] **Step 1: Write the failing test**

```csharp
// src/WindowsFormsApp3.Tests/Utils/PdfCutPathExtractorTests.cs
using System;
using System.IO;
using Xunit;
using WindowsFormsApp3.Services;

namespace WindowsFormsApp3.Tests.Utils
{
    public class PdfCutPathExtractorTests
    {
        [Fact]
        public void CanExtractCutPathFromLastPage_NullFile_ReturnsFalse()
        {
            bool result = PdfCutPathExtractor.CanExtractCutPathFromLastPage(null);
            Assert.False(result);
        }

        [Fact]
        public void CanExtractCutPathFromLastPage_NonExistentFile_ReturnsFalse()
        {
            bool result = PdfCutPathExtractor.CanExtractCutPathFromLastPage("nonexistent.pdf");
            Assert.False(result);
        }

        [Fact]
        public void ExtractAndConvertCutPath_NullFile_ReturnsNull()
        {
            float[] bounds;
            byte[] result = PdfCutPathExtractor.ExtractAndConvertCutPath(null, null, out bounds);
            Assert.Null(result);
        }
    }
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `dotnet test src/WindowsFormsApp3.Tests/ --filter "FullyQualifiedName~PdfCutPathExtractorTests" -v n`
Expected: FAIL with "PdfCutPathExtractor does not exist"

- [ ] **Step 3: Create PdfCutPathExtractor.cs with method stubs**

```csharp
// src/WindowsFormsApp3/Services/PdfCutPathExtractor.cs
using System;
using System.IO;
using System.Collections.Generic;
using iText.Kernel.Pdf;

namespace WindowsFormsApp3.Services
{
    /// <summary>
    /// PDF裁切路径提取器 - 从PDF最后一页提取裁切路径
    /// </summary>
    public static class PdfCutPathExtractor
    {
        /// <summary>
        /// 检测PDF文件最后一页是否有可提取的裁切路径
        /// </summary>
        public static bool CanExtractCutPathFromLastPage(string filePath)
        {
            try
            {
                if (string.IsNullOrEmpty(filePath) || !File.Exists(filePath)) return false;
                using (var reader = new PdfReader(filePath))
                using (var doc = new PdfDocument(reader))
                {
                    if (doc.GetNumberOfPages() < 2) return false;
                    var lastPage = doc.GetPage(doc.GetNumberOfPages());
                    float[] bounds;
                    byte[] pathBytes = ExtractAndConvertCutPath(lastPage, doc, out bounds);
                    return pathBytes != null && pathBytes.Length > 0;
                }
            }
            catch (Exception ex) { LogHelper.Debug("CanExtractCutPathFromLastPage: " + ex.Message); return false; }
        }

        /// <summary>
        /// 从PDF页面中提取裁切路径，并将坐标转换到页面坐标系
        /// </summary>
        public static byte[] ExtractAndConvertCutPath(PdfPage page, PdfDocument doc, out float[] pageBounds)
        {
            // TODO: 从 PdfTools.cs 迁移实现
            pageBounds = null;
            return null;
        }

        /// <summary>
        /// 转换路径坐标（应用cm偏移）
        /// </summary>
        internal static string ConvertLineCoords(string line, float cmTx, float cmTy)
        {
            // TODO: 从 PdfTools.cs 迁移实现
            return line;
        }

        /// <summary>
        /// 更新路径边界
        /// </summary>
        internal static void UpdateBounds(string line, ref float minX, ref float minY, ref float maxX, ref float maxY)
        {
            // TODO: 从 PdfTools.cs 迁移实现
        }
    }
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `dotnet test src/WindowsFormsApp3.Tests/ --filter "FullyQualifiedName~PdfCutPathExtractorTests" -v n`
Expected: PASS

- [ ] **Step 5: 从 PdfTools.cs 迁移 ExtractAndConvertCutPath 方法**

将 `PdfTools.cs` 中的以下方法移到 `PdfCutPathExtractor.cs`：
- `ExtractAndConvertCutPath` (line 376-465)
- `ConvertLineCoords` (line 468-482)
- `UpdateBounds` (line 482-492)
- `ComputeOffsetBleedPath` 及相关辅助方法
- `PathSeg` 结构体

同时更新 `PdfTools.cs` 中的调用方，改为调用 `PdfCutPathExtractor`。

- [ ] **Step 6: Run all tests to verify no regression**

Run: `dotnet test src/WindowsFormsApp3.Tests/ -v n`
Expected: All existing tests pass + new PdfCutPathExtractorTests pass

- [ ] **Step 7: Commit**

```bash
git add src/WindowsFormsApp3/Services/PdfCutPathExtractor.cs src/WindowsFormsApp3/Utils/PdfTools.cs src/WindowsFormsApp3.Tests/Utils/PdfCutPathExtractorTests.cs
git commit -m "refactor: 从PdfTools提取裁切路径提取逻辑到PdfCutPathExtractor"
```

---

## Task 2: 从 PdfTools.cs 提取异形处理逻辑

**Files:**
- Create: `src/WindowsFormsApp3/Services/PdfSpecialShapeProcessor.cs`
- Modify: `src/WindowsFormsApp3/Utils/PdfTools.cs:495-720`
- Test: `src/WindowsFormsApp3.Tests/Utils/PdfSpecialShapeProcessorTests.cs`

- [ ] **Step 1: Write the failing test**

```csharp
// src/WindowsFormsApp3.Tests/Utils/PdfSpecialShapeProcessorTests.cs
using System;
using System.IO;
using Xunit;
using WindowsFormsApp3.Services;

namespace WindowsFormsApp3.Tests.Utils
{
    public class PdfSpecialShapeProcessorTests
    {
        [Fact]
        public void ProcessSpecialShapePdf_NullFile_ReturnsFalse()
        {
            bool result = PdfSpecialShapeProcessor.ProcessSpecialShapePdf(null);
            Assert.False(result);
        }

        [Fact]
        public void ProcessSpecialShapePdf_NonExistentFile_ReturnsFalse()
        {
            bool result = PdfSpecialShapeProcessor.ProcessSpecialShapePdf("nonexistent.pdf");
            Assert.False(result);
        }

        [Fact]
        public void ProcessSpecialShapePdf_NonPdfFile_ReturnsFalse()
        {
            string tmpFile = Path.Combine(Path.GetTempPath(), "test.txt");
            File.WriteAllText(tmpFile, "not a pdf");
            bool result = PdfSpecialShapeProcessor.ProcessSpecialShapePdf(tmpFile);
            Assert.False(result);
            File.Delete(tmpFile);
        }
    }
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `dotnet test src/WindowsFormsApp3.Tests/ --filter "FullyQualifiedName~PdfSpecialShapeProcessorTests" -v n`
Expected: FAIL with "PdfSpecialShapeProcessor does not exist"

- [ ] **Step 3: Create PdfSpecialShapeProcessor.cs with ProcessSpecialShapePdf 方法**

从 `PdfTools.cs` 迁移 `ProcessSpecialShapePdf` 方法 (line 495-720) 到新类。

- [ ] **Step 4: Run test to verify it passes**

Run: `dotnet test src/WindowsFormsApp3.Tests/ --filter "FullyQualifiedName~PdfSpecialShapeProcessorTests" -v n`
Expected: PASS

- [ ] **Step 5: 更新 PdfTools.cs 中的调用方**

将 `AddDotsAddCounterLayer` 中的 `ProcessSpecialShapePdf` 调用改为 `PdfSpecialShapeProcessor.ProcessSpecialShapePdf`。

- [ ] **Step 6: Run all tests**

Run: `dotnet test src/WindowsFormsApp3.Tests/ -v n`
Expected: All tests pass

- [ ] **Step 7: Commit**

```bash
git add src/WindowsFormsApp3/Services/PdfSpecialShapeProcessor.cs src/WindowsFormsApp3/Utils/PdfTools.cs src/WindowsFormsApp3.Tests/Utils/PdfSpecialShapeProcessorTests.cs
git commit -m "refactor: 从PdfTools提取异形处理逻辑到PdfSpecialShapeProcessor"
```

---

## Task 3: 合并重复的 LoggerAdapter

**Files:**
- Modify: `src/WindowsFormsApp3/Services/LoggerAdapter.cs` - 保留此文件
- Delete: `src/WindowsFormsApp3/Utils/LoggerAdapter.cs` - 删除重复文件
- Test: `src/WindowsFormsApp3.Tests/Utils/LoggerAdapterTests.cs` (如有)

- [ ] **Step 1: 检查两个 LoggerAdapter 的内容差异**

```bash
diff src/WindowsFormsApp3/Utils/LoggerAdapter.cs src/WindowsFormsApp3/Services/LoggerAdapter.cs
```

- [ ] **Step 2: 确认哪个版本更完整**

通常 `Services/` 目录下的版本更完整（有实际的日志实现）。确认后删除 `Utils/` 下的重复版本。

- [ ] **Step 3: 检查是否有代码引用被删除的文件**

```bash
rg "using.*Utils.*LoggerAdapter" --type cs src/
rg "Utils.LoggerAdapter" --type cs src/
```

如有引用，更新为 `Services.LoggerAdapter`。

- [ ] **Step 4: 删除重复文件**

```bash
git rm src/WindowsFormsApp3/Utils/LoggerAdapter.cs
```

- [ ] **Step 5: Run all tests**

Run: `dotnet test src/WindowsFormsApp3.Tests/ -v n`
Expected: All tests pass

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "refactor: 合并重复的LoggerAdapter，删除Utils目录下的副本"
```

---

## Task 4: 修复空 catch 块

**Files:**
- Modify: `src/WindowsFormsApp3/Utils/ITextSharpLGPLPdfTools.cs:146`
- Modify: `src/WindowsFormsApp3/Utils/PdfTools.cs:1649`
- Modify: `src/WindowsFormsApp3/Utils/ThemeHelper.cs:457,462,541,543`
- Modify: `src/WindowsFormsApp3/Forms/Utils/FloatingDropZoneForm.cs:140,255,1415,1518`

- [ ] **Step 1: 为每个空 catch 块添加日志**

对每个 `catch { }` 替换为 `catch (Exception ex) { LogHelper.Debug("操作失败: " + ex.Message); }`

具体修改：
- `ITextSharpLGPLPdfTools.cs:146`: `catch { }` → `catch (Exception ex) { LogHelper.Debug("PDF处理异常: " + ex.Message); }`
- `PdfTools.cs:1649`: 同上
- `ThemeHelper.cs` 4处: 同上
- `FloatingDropZoneForm.cs` 4处: 同上

- [ ] **Step 2: Run all tests**

Run: `dotnet test src/WindowsFormsApp3.Tests/ -v n`
Expected: All tests pass

- [ ] **Step 3: Commit**

```bash
git add src/WindowsFormsApp3/Utils/ITextSharpLGPLPdfTools.cs src/WindowsFormsApp3/Utils/PdfTools.cs src/WindowsFormsApp3/Utils/ThemeHelper.cs src/WindowsFormsApp3/Forms/Utils/FloatingDropZoneForm.cs
git commit -m "fix: 为10处空catch块添加异常日志记录"
```

---

## Task 5: 清理重复接口定义

**Files:**
- Delete: `src/WindowsFormsApp3/Services/IConfigService.cs`
- Delete: `src/WindowsFormsApp3/Services/IFileMonitor.cs`
- Delete: `src/WindowsFormsApp3/Services/IPdfProcessingService.cs`
- Modify: 所有引用这些接口的文件

- [ ] **Step 1: 检查重复接口的内容是否一致**

```bash
diff src/WindowsFormsApp3/Interfaces/IConfigService.cs src/WindowsFormsApp3/Services/IConfigService.cs
diff src/WindowsFormsApp3/Interfaces/IFileMonitor.cs src/WindowsFormsApp3/Services/IFileMonitor.cs
diff src/WindowsFormsApp3/Interfaces/IPdfProcessingService.cs src/WindowsFormsApp3/Services/IPdfProcessingService.cs
```

- [ ] **Step 2: 更新引用，统一使用 Interfaces/ 目录下的版本**

```bash
rg "using.*Services.*IConfigService" --type cs src/
rg "using.*Services.*IFileMonitor" --type cs src/
rg "using.*Services.*IPdfProcessingService" --type cs src/
```

将所有 `Services.IConfigService` 引用改为 `Interfaces.IConfigService`（同理其他两个）。

- [ ] **Step 3: 删除重复文件**

```bash
git rm src/WindowsFormsApp3/Services/IConfigService.cs
git rm src/WindowsFormsApp3/Services/IFileMonitor.cs
git rm src/WindowsFormsApp3/Services/IPdfProcessingService.cs
```

- [ ] **Step 4: Run all tests**

Run: `dotnet test src/WindowsFormsApp3.Tests/ -v n`
Expected: All tests pass

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "refactor: 统一接口定义到Interfaces目录，删除Services下的重复文件"
```

---

## Task 6: 验证与清理

- [ ] **Step 1: 全量构建验证**

```bash
dotnet build WindowsFormsApp3.sln -c Release --no-incremental
```
Expected: 0 errors

- [ ] **Step 2: 全量测试验证**

```bash
dotnet test src/WindowsFormsApp3.Tests/ -v n
```
Expected: All tests pass (pre-existing failures excluded)

- [ ] **Step 3: 代码审查确认**

检查拆分后的文件职责是否清晰，无循环依赖。

- [ ] **Step 4: 最终提交**

```bash
git add -A
git commit -m "chore: 代码质量改进完成 - 拆分God class、合并重复定义、修复空catch"
```

---

## 依赖关系

```
Task 1 (PdfCutPathExtractor) ──┐
Task 2 (PdfSpecialShapeProcessor) ├── 可并行
Task 3 (LoggerAdapter合并) ────────┘
                                    ↓
Task 4 (空catch修复) ────────────── 可并行
Task 5 (重复接口清理) ──────────────┘
                                    ↓
                            Task 6 (验证)
```

## 风险与缓解

| 风险 | 影响 | 缓解措施 |
|---|---|---|
| 拆分后方法调用失败 | 编译错误 | 每个 Task 后运行全量测试 |
| 接口删除导致引用断裂 | 编译错误 | Step 2 先搜索所有引用 |
| 空catch修复引入新异常 | 运行时错误 | 日志级别用 Debug，不影响功能 |
