# ContextMenuStrip 与短确认弹框迁移访谈记录

## 元数据

- 时间：2026-09-07
- 模式：deep-interview / standard
- 项目类型：brownfield
- 最终不确定性：8%（阈值：20%）
- 上下文快照：`.omx/context/context-menu-modal-migration-20260907T172853Z.md`
- 状态持久化：`omx state write` 因当前会话状态域不可写而未能初始化；本访谈记录为可交接的静态产物。

## 访谈摘要

1. 用户目标：统一视觉风格。
2. 迁移范围：仅迁移应用内业务右键菜单与简短确认弹框。
3. 非目标：系统托盘菜单、文件选择器、复杂配置窗体不纳入本次迁移。
4. 业务边界：业务功能不改变；菜单项、命令、排序、快捷键、启用/禁用条件与确认后的业务结果均需保持原状。
5. 验收：迁移后的界面必须同时适配深色、浅色、护眼绿、经典蓝主题；图标、圆角、阴影、主/危险操作色与禁用态一致。
6. 交付边界：覆盖所有已识别的应用内业务右键菜单与简短确认弹框，而不是只做高频模块试点。

## 压力测试

针对批量重命名中的删除、覆盖、剪切等高风险命令，确认了核心边界：迁移不得改变业务功能。因此风险语义的视觉增强不能改变原有默认行为、快捷键或命令执行条件。
